spring:
  profiles:
    active: euk1
  #zipkin
  zipkin:
    base-url: http://localhost:9411/
    #采样比例1
  sleuth:
    sampler:
      rate: 1




# 设置以哪个配置文件启动   idea program agu  --spring.profiles.active=euk1
# 设置启动的端口  Environment variables   server.port=8001


logging:
  level:
    root: INFO
    org.springframework.web.servlet.DispatcherServlet: DEBUG
    org.springframework.cloud.sleuth: DEBUG



